class ShiftRules {
  final bool hasNightDifferentiation;
  final bool hasHolidayBonus;
  final bool hasExternalService;

  const ShiftRules({
    required this.hasNightDifferentiation,
    required this.hasHolidayBonus,
    required this.hasExternalService,
  });
}